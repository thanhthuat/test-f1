import React from "react";

type Props = {};

const HeaderContainer = (props: Props) => {
  return (
    <header className="clsheader">
      <nav className="clsheader-content">
        <img></img>
        Page result F1
      </nav>
    </header>
  );
};

export default HeaderContainer;
